[tool.coverage.run]
omit = ["tests/*"]
relative_files = true

[tool.mypy]
files = ["alembic", "lofi", "tests"]
strict = true

[tool.pytest.ini_options]
env = ["AWS_BUCKET_NAME=aws_bucket_name", "DB_NAME=db_name"]
testpaths = ["tests"]
