[tool.coverage.run]
omit = ["tests/*"]
relative_files = true

[tool.mypy]
files = ["alembic", "lofi", "tests"]
strict = true

[tool.pytest.ini_options]
env = [
    'AWS_ACCESS_KEY_ID=""',
    'AWS_DEFAULT_REGION=""',
    'AWS_SECRET_ACCESS_KEY=""'
]
testpaths = ["tests"]
